using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundEffectController : MonoBehaviour
{
    /*
    protected AudioSource crunch;
    

    public GameObject Player1;
    public GameObject Player2;
    public TagFatMechanic TF1Script;
    public TagFatMechanic TF2Script;

    public Timer Timer1;
    public Timer Timer2;

    

    // Start is called before the first frame update
    void Start()
    {
        crunch = GetComponent<AudioSource>();
        
    }

    // Update is called once per frame
    void Update()
    {
        


        
        if (Timer1.remaining1Time == 35  || Timer1.remaining1Time == 30 || Timer1.remaining1Time == 25 || Timer1.remaining1Time == 20 || Timer1.remaining1Time == 15 || Timer1.remaining1Time == 10 || Timer1.remaining1Time == 5 || Timer1.remaining1Time == 0)
        {
            crunch.Play();
        }

        if (Timer2.remaining1Time == 35 || Timer2.remaining1Time == 30 || Timer2.remaining1Time == 25 || Timer2.remaining1Time == 20 || Timer2.remaining1Time == 15 || Timer2.remaining1Time == 10 || Timer2.remaining1Time == 5 || Timer2.remaining1Time == 0)
        {
            crunch.Play();
        }
        

        //StartCoroutine(bite());
        

    }
    
    public IEnumerator bite()
    {
        while (TF1Script.copHasDonut)
        {
            yield return new WaitForSeconds(5f);
            crunch.Play();
            if (Timer1.remaining1Time == 0)
            {
                break;
            }
        }

        while (TF2Script.prisonerHasDonut)
        {
            yield return new WaitForSeconds(5f);
            crunch.Play();
            if (Timer2.remaining2Time == 0)
            {
                break;
            }
        }
    }*/
    
    }
